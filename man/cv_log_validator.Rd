% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_log_validator.R
\name{cv_log_validator}
\alias{cv_log_validator}
\title{Validate a Random Forest Model for Logarithmically Transformed Targets}
\usage{
cv_log_validator(
  data,
  target_var,
  k = 5,
  seed = 1,
  metrics = c("RMSE", "MAE", "MAPE")
)
}
\arguments{
\item{data}{A data frame containing the features and target variable.}

\item{target_var}{The name of the target variable column in the data frame.}

\item{k}{The number of folds for k-fold cross-validation. Default is 5.}

\item{seed}{The seed for random number generation. Default is 1.}

\item{metrics}{A vector of metrics to be calculated. Possible values are 'RMSE', 'MAE', and 'MAPE'. Default is all.}
}
\value{
A list containing the calculated metrics for both the logarithmic and raw scales.
}
\description{
This function performs k-fold cross-validation on a random forest model.
It is specifically designed for target variables that are logarithmically transformed.
The function returns various evaluation metrics for both the logarithmic and raw scales.
}
\note{
This function is optimized for target variables that are logarithmically transformed.
}
\examples{
\dontrun{
data <- data.frame(x1 = rnorm(100), x2 = rnorm(100), y = rnorm(100))
result <- cv_log_validator(data, "y")
}
}
\seealso{
\code{\link[caret]{createFolds}}, \code{\link[ranger]{ranger}}
}
